# CWLLG3549E Failed to install the snapshot with ID 0 because the snapshot was developed using Workflow Center version 1 which is incompatible with the current workflow server version 2.

## Explanation

## User response