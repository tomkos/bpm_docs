# CWZMU0063E The XML map 0 that is associated with the XSLT mediation primitive 1 will not run in the lazy business object parsing mode. Use Quick Fix to migrate the map so that it can run in both eager and lazy parsing modes.

## Explanation

## User response