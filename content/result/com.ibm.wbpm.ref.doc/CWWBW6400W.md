# CWWBW6400W Assigning the xsd:anyType typed from-variable ''0'' to the xsd:anySimpleType typed scope variable ''1'' might result in a runtime error (4 scope activity, 2 from XSD type, to 3 XSD type).

## Explanation

## User response