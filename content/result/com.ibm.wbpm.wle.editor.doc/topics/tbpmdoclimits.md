# Limitations in working with BPM documents

Some known limitations are:

- The BPM document store restricts
document size to 1 GB (gigabyte) or less
- The BPM document store is
not available on all platforms
- The BPM document store should not be configured with
additional ECM services or add-ons

## The BPM document store restricts document
size to 1GB or less

## The BPM document store is not
available on all platforms

## The BPM document store should not be configured
with additional ECM services or add-ons

You should not use ECM services or add-ons that are not specified for use with the BPM document store, such as using IBM® Enterprise Records against an object store to control the lifecycle of
a document.